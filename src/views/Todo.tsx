import { createResource, createSignal, For, Show, onMount } from "solid-js";
import { personalApi, type Todo as TodoItem } from "../api/personal";
import "./Todo.css";
import { ProfilePicker } from "../components/Pickers";
import { profileId, profiles, reloadProfiles } from "../session";
import { humanError } from "../session";

const blank = () => ({ content:"", due_date:"", source_entity_type:"", source_entity_id:"", assignee_ids:[] as string[] });
export default function Todo() {
  onMount(()=>{ void reloadProfiles(); });
  const [form,setForm]=createSignal(blank()); const [error,setError]=createSignal("");
  const [todos,{refetch}]=createResource(profileId,id=>id?personalApi.todos(id,true):Promise.resolve([]));
  const active=()=>profiles()?.filter(p=>!p.archived)??[];
  const nameOf=(id:string)=>{ const p=active().find(x=>x.id===id); return p?(p.display_name||p.username):id; };
  const addAssignee=(id:string)=>{ if(!id) return; const f=form(); if(f.assignee_ids.includes(id)) return; setForm({...f,assignee_ids:[...f.assignee_ids,id]}); };
  const removeAssignee=(id:string)=>{ const f=form(); setForm({...f,assignee_ids:f.assignee_ids.filter(x=>x!==id)}); };
  const save=async(e:SubmitEvent)=>{ e.preventDefault(); try { if(!profileId().trim()||!form().content.trim()) throw new Error("Pick a profile and enter task content."); const f=form(); if(Boolean(f.source_entity_type)!==Boolean(f.source_entity_id)) throw new Error("Source type and source ID must be supplied together."); await personalApi.createTodo({profile_id:profileId().trim(),content:f.content.trim(),due_date:f.due_date||null,done:false,source_entity_type:f.source_entity_type||null,source_entity_id:f.source_entity_id||null,assignee_ids:f.assignee_ids}); setForm(blank()); refetch(); } catch(reason) { setError(humanError(reason)); } };
  const update=async(todo:TodoItem, patch:Partial<TodoItem>)=>{ try { await personalApi.updateTodo({...todo,...patch}); refetch(); } catch(reason) { setError(humanError(reason)); } };
  return <section class="personal-view"><header><div><h1>My tasks</h1><p>Personal tasks with optional bookmarks back to Space entities.</p></div><ProfilePicker/></header><Show when={error()}><p class="personal-error">{error()}</p></Show><div class="todo-layout"><form class="personal-card todo-form" onSubmit={save}><div class="todo-form-head"><h2>New task</h2><p>Capture something to do and assign it in seconds.</p></div><label class="todo-field todo-field-main"><span class="field-label">Task</span><input autofocus placeholder="What needs doing?" value={form().content} onInput={e=>setForm({...form(),content:e.currentTarget.value})}/></label><div class="todo-field-row"><label class="todo-field"><span class="field-label">Due date</span><input type="date" value={form().due_date} onInput={e=>setForm({...form(),due_date:e.currentTarget.value})}/></label><label class="todo-field"><span class="field-label">Assignees</span><select value="" onChange={e=>{addAssignee(e.currentTarget.value);e.currentTarget.value="";}}><option value="">Add assignee…</option><For each={active().filter(p=>!form().assignee_ids.includes(p.id))}>{p=><option value={p.id}>{p.display_name||p.username}</option>}</For></select></label></div><Show when={form().assignee_ids.length}><ul class="assignee-chips"><For each={form().assignee_ids}>{id=><li class="assignee-chip">{nameOf(id)}<button type="button" aria-label={`Remove ${nameOf(id)}`} onClick={()=>removeAssignee(id)}>×</button></li>}</For></ul></Show><details class="todo-source"><summary>Source bookmark</summary><label class="todo-field"><span class="field-label">Entity type</span><input placeholder="issue, document…" value={form().source_entity_type} onInput={e=>setForm({...form(),source_entity_type:e.currentTarget.value})}/></label><label class="todo-field"><span class="field-label">Entity ID</span><input placeholder="Entity ID" value={form().source_entity_id} onInput={e=>setForm({...form(),source_entity_id:e.currentTarget.value})}/></label></details><button class="primary todo-submit">Add task</button></form><div class="todo-list"><Show when={!profileId()}><p class="personal-empty">No profile selected — add one in Members.</p></Show><For each={todos()}>{todo=><article classList={{"todo-row":true,done:todo.done}}><input aria-label={`Mark ${todo.content} done`} type="checkbox" checked={todo.done} onChange={e=>update(todo,{done:e.currentTarget.checked})}/><div><strong>{todo.content}</strong><Show when={todo.due_date}><time>Due {todo.due_date}</time></Show><Show when={todo.assignee_ids.length}><ul class="assignee-chips readonly"><For each={todo.assignee_ids}>{id=><li class="assignee-chip">{nameOf(id)}</li>}</For></ul></Show><Show when={todo.source_entity_type}><small>{todo.source_entity_type}: {todo.source_entity_id}</small></Show></div><button class="ghost" title="Delete task" onClick={async()=>{try{await personalApi.deleteTodo(todo.id);refetch()}catch(reason){setError(humanError(reason))}}}>×</button></article>}</For></div></div></section>;
}
