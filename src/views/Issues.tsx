import { createResource, createSignal, For, Show } from "solid-js";
import { planningApi, type Issue, type Status } from "../api/issues";
import "./Issues.css";

const blank = () => ({ project_id:"", title:"", description:"", status_id:"", assignee_id:"", due_date:"" });
export default function Issues() {
  const [projectId,setProjectId]=createSignal(""); const [query,setQuery]=createSignal(""); const [statusFilter,setStatusFilter]=createSignal(""); const [selected,setSelected]=createSignal<Issue>(); const [form,setForm]=createSignal(blank()); const [error,setError]=createSignal(""); const [tagName,setTagName]=createSignal(""); const [checklistTitle,setChecklistTitle]=createSignal(""); const [minutes,setMinutes]=createSignal("");
  const [issues,{refetch:reloadIssues}]=createResource(()=>[projectId(),query(),statusFilter()] as const,([project_id,text,status_id])=>planningApi.issues({project_id:project_id||undefined,text:text||undefined,status_id:status_id||undefined}));
  const [statuses,{refetch:reloadStatuses}]=createResource(projectId, id=>id?planningApi.statuses(id):Promise.resolve([]));
  const [detail,{refetch:reloadDetail}]=createResource(()=>selected()?.id, id=>id?planningApi.issue(id):Promise.resolve(null));
  const saveIssue=async(e:SubmitEvent)=>{e.preventDefault();try { const f=form(); if(!f.project_id||!f.title.trim()) throw new Error("Project ID and title are required."); const item=await planningApi.createIssue({project_id:f.project_id,title:f.title.trim(),description:f.description||null,status_id:f.status_id||null,assignee_id:f.assignee_id||null,created_by:null,due_date:f.due_date||null,archived:false}); setSelected(item);setForm(blank());reloadIssues(); }catch(e){setError(String(e))}};
  const edit=async(e:SubmitEvent)=>{e.preventDefault();const i=selected();if(!i)return;try{await planningApi.updateIssue(i);reloadIssues();reloadDetail()}catch(e){setError(String(e))}};
  const saveStatus=async()=>{const name=prompt("Status name");if(!name||!projectId())return;try{await planningApi.createStatus({project_id:projectId(),name,color:"#5b78e5",resolved:false});reloadStatuses()}catch(e){setError(String(e))}};
  const addTag=async()=>{const i=selected();if(!i||!tagName().trim())return;try{const tag=await planningApi.saveTag({project_id:i.project_id,parent_id:null,name:tagName().trim(),archived:false});await planningApi.setTags(i.id,[...(detail()?.tags.map(t=>t.id)??[]),tag.id]);setTagName("");reloadDetail();reloadIssues()}catch(e){setError(String(e))}};
  const addChecklist=async()=>{const i=selected();if(!i||!checklistTitle().trim())return;try{await planningApi.saveChecklist({issue_id:i.id,title:checklistTitle().trim()});setChecklistTitle("");reloadDetail()}catch(e){setError(String(e))}};
  const logTime=async()=>{const i=selected();const duration_minutes=Number(minutes());if(!i||!Number.isInteger(duration_minutes)||duration_minutes<1)return;try{await planningApi.saveTime({issue_id:i.id,profile_id:i.assignee_id||i.created_by||"default-org",entry_date:new Date().toISOString().slice(0,10),duration_minutes,description:null});setMinutes("");reloadDetail()}catch(e){setError(String(e))}};
  return <section class="planning-view"><header class="planning-head"><div><h1>Issues</h1><p>Track project work, status, tags, checklists, time, and sub-items.</p></div><label>Project ID <input value={projectId()} onInput={e=>setProjectId(e.currentTarget.value)} placeholder="Select a project ID" /></label></header><Show when={error()}><p class="planning-error">{error()}</p></Show><div class="issue-layout"><aside class="issue-sidebar"><form class="new-issue" onSubmit={saveIssue}><h2>New issue</h2><input placeholder="Project ID" value={form().project_id} onInput={e=>setForm({...form(),project_id:e.currentTarget.value})}/><input placeholder="Title" value={form().title} onInput={e=>setForm({...form(),title:e.currentTarget.value})}/><textarea placeholder="Description" value={form().description} onInput={e=>setForm({...form(),description:e.currentTarget.value})}/><select value={form().status_id} onChange={e=>setForm({...form(),status_id:e.currentTarget.value})}><option value="">No status</option><For each={statuses()}>{s=><option value={s.id}>{s.name}</option>}</For></select><input placeholder="Assignee profile ID" value={form().assignee_id} onInput={e=>setForm({...form(),assignee_id:e.currentTarget.value})}/><input type="date" value={form().due_date} onInput={e=>setForm({...form(),due_date:e.currentTarget.value})}/><button class="primary">Create issue</button></form><section class="status-editor"><div class="section-title"><h2>Statuses</h2><button onClick={saveStatus}>+</button></div><For each={statuses()}>{(s:Status)=><div class="status-row"><input type="color" value={s.color} onInput={async e=>{await planningApi.updateStatus({...s,color:e.currentTarget.value});reloadStatuses()}}/><input value={s.name} onChange={async e=>{await planningApi.updateStatus({...s,name:e.currentTarget.value});reloadStatuses()}}/><label><input type="checkbox" checked={s.resolved} onChange={async e=>{await planningApi.updateStatus({...s,resolved:e.currentTarget.checked});reloadStatuses()}}/> done</label><button class="ghost" onClick={async()=>{try{await planningApi.deleteStatus(s.id);reloadStatuses()}catch(e){setError(String(e))}}}>×</button></div>}</For><Show when={!projectId()}><p class="hint">Enter a project ID to edit its statuses.</p></Show></section></aside><main class="issue-list-pane"><div class="filter-row"><input placeholder="Search title or description" value={query()} onInput={e=>setQuery(e.currentTarget.value)}/><select value={statusFilter()} onChange={e=>setStatusFilter(e.currentTarget.value)}><option value="">All statuses</option><For each={statuses()}>{s=><option value={s.id}>{s.name}</option>}</For></select></div><Show when={issues.loading}><p class="hint">Loading issues…</p></Show><ul class="issue-list"><For each={issues()}>{issue=><li classList={{active:selected()?.id===issue.id}} onClick={()=>setSelected(issue)}><span class="issue-number">#{issue.number}</span><strong>{issue.title}</strong><span class="status-dot" style={{background:statuses()?.find(s=>s.id===issue.status_id)?.color}}/><Show when={issue.due_date}><time>{issue.due_date}</time></Show></li>}</For></ul></main><aside class="issue-detail"><Show when={selected()} fallback={<p class="hint pad">Select an issue to view details.</p>}>{issue=><><form onSubmit={edit}><div class="section-title"><span>#{issue().number}</span><button type="button" class="ghost" onClick={async()=>{await planningApi.archiveIssue(issue().id,!issue().archived);reloadIssues();reloadDetail()}}>{issue().archived?"Restore":"Archive"}</button></div><input class="issue-title-input" value={issue().title} onInput={e=>setSelected({...issue(),title:e.currentTarget.value})}/><textarea value={issue().description??""} onInput={e=>setSelected({...issue(),description:e.currentTarget.value||null})}/><label>Status<select value={issue().status_id??""} onChange={e=>setSelected({...issue(),status_id:e.currentTarget.value||null})}><option value="">No status</option><For each={statuses()}>{s=><option value={s.id}>{s.name}</option>}</For></select></label><label>Assignee<input value={issue().assignee_id??""} onInput={e=>setSelected({...issue(),assignee_id:e.currentTarget.value||null})}/></label><label>Due date<input type="date" value={issue().due_date??""} onInput={e=>setSelected({...issue(),due_date:e.currentTarget.value||null})}/></label><button class="primary">Save</button></form><section><h3>Tags</h3><div class="chips"><For each={detail()?.tags}>{tag=><span>{tag.name}</span>}</For></div><div class="inline-form"><input placeholder="New tag" value={tagName()} onInput={e=>setTagName(e.currentTarget.value)}/><button onClick={addTag}>Add</button></div></section><section><h3>Checklists</h3><For each={detail()?.checklists}>{list=><ChecklistList id={list.id} title={list.title}/>}</For><div class="inline-form"><input placeholder="Checklist title" value={checklistTitle()} onInput={e=>setChecklistTitle(e.currentTarget.value)}/><button onClick={addChecklist}>Add</button></div></section><section><h3>Time tracking <small>{detail()?.time_total_minutes??0} min</small></h3><div class="inline-form"><input type="number" min="1" placeholder="Minutes" value={minutes()} onInput={e=>setMinutes(e.currentTarget.value)}/><button onClick={logTime}>Log</button></div></section><section><h3>Sub-items</h3><For each={detail()?.children}>{child=><p>#{child.number} {child.title}</p>}</For></section></>}</Show></aside></div></section>;
}
function ChecklistList(props:{id:string;title:string}) { const [items,{refetch}]=createResource(()=>planningApi.items(props.id)); const [text,setText]=createSignal(""); const add=async()=>{if(!text().trim())return;await planningApi.saveItem({checklist_id:props.id,parent_id:null,item_text:text().trim(),item_done:false});setText("");refetch()};return <div class="checklist"><strong>{props.title}</strong><For each={items()}>{item=><label><input type="checkbox" checked={item.item_done} onChange={async e=>{await planningApi.toggleItem(item.id,e.currentTarget.checked);refetch()}}/>{item.item_text}</label>}</For><div class="inline-form"><input placeholder="Item" value={text()} onInput={e=>setText(e.currentTarget.value)}/><button onClick={add}>+</button></div></div> }
