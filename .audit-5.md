# Issue #5 · native/server audit (Kali)

Scope→read-only source audit; no client implementation diagnosis. `src-tauri/migrations/` is absent in this checkout. Migrations are inline in `src-tauri/src/db.rs`; head=`SCHEMA_VERSION=138` (`db.rs:60`).

## 1. TS ↔ Rust ↔ DB

| field | TS `Meeting` | Rust `Meeting` | DB `meetings` | result |
|---|---|---|---|---|
| id | `string` required | `String` required | `TEXT PRIMARY KEY` | exact |
| title | `string` required | `String` required | `TEXT NOT NULL` | exact |
| description | `string|null` required | `Option<String>` required-key | `TEXT NULL` | exact value; Rust key is non-default |
| starts_at / ends_at | `number` required | `i64` required | `INTEGER NOT NULL` | exact |
| rrule / location | `string|null` required | `Option<String>` required-key | `TEXT NULL` | exact value; Rust keys non-default |
| organizer_id / channel_id | `string|null` required | `Option<String>` required-key | `TEXT NULL REFERENCES profiles/channels(id)` | exact value; FK can reject non-existent non-null id |
| visibility | union required | `String`, serde default `participants` | `TEXT NOT NULL DEFAULT participants`, CHECK | TS stricter presence |
| modification_preference | union required | `String`, serde default `organizer-only` | `TEXT NOT NULL DEFAULT organizer-only`, CHECK | TS stricter presence |
| archived | `boolean` required | `bool` required | `INTEGER NOT NULL DEFAULT 0` | exact |
| video_provider | `livekit|null` required | `Option<String>`, serde default | `TEXT NULL` | TS stricter presence; Rust validates livekit only |
| video_room_id / join_url | `string|null` required | `Option<String>`, serde default | `TEXT NULL` | TS stricter presence; create does not insert either (NULL/default) |
| meeting_url | `string|null` required | `Option<String>`, serde default | `TEXT NULL` | TS stricter presence; native validates http(s) |
| video_status | union required | `String`, serde default `scheduled` | `TEXT NOT NULL DEFAULT scheduled` | TS stricter presence |
| video_started_at / video_ended_at | `number|null` required | `Option<i64>`, serde default | `INTEGER NULL` | TS stricter presence; create does not insert (NULL) |
| video_ended_by | `string|null` required | `Option<String>`, serde default | `TEXT NULL REFERENCES profiles(id)` | TS stricter presence; create does not insert (NULL) |
| source_entity_type / source_entity_id | `string|null` required | `Option<String>`, serde default | `TEXT NULL` | TS stricter presence; native requires both-or-neither |

Truth sources→`src/api/meetings.ts:15`; `src-tauri/src/meetings.rs:29-73`; base table `src-tauri/src/db.rs:1412`; additive columns: V75 `db.rs:822-831`, V101 `973-976`, V129 `836-844`, source anchors effective when old version `<134` `886-902`, `meeting_url` V135 `1014-1016`. Note: source-anchor comment labels itself V133 but condition is `<134`; treat migration number as **V134 by executable condition**. No field-name mismatch found.

## 2. Concrete refusal/failure conditions

| condition | native/server site |
|---|---|
| missing/empty title | `meetings.rs:154-155` |
| end <= start | `meetings.rs:156-158` |
| only one source anchor half | `meetings.rs:120-124`, `159` |
| meeting URL not http(s), missing host | `meetings.rs:131-147`, `160` |
| invalid RRULE component/frequency/non-positive interval/count | `meetings.rs:620-651`, invoked `161-163` |
| invalid video status/provider | `meetings.rs:164-171` |
| invalid visibility/edit preference | `meetings.rs:172-179` |
| duplicate id; non-null organizer/channel FK invalid; missing expected DB columns | INSERT SQLite error, `meetings.rs:385`; FK enforced `db.rs:65-79` |
| no DB binding/path or migration/open failure | `db.rs:131-138` |
| `channel_id=None`: post-insert automatic entity-channel creation can error; prior INSERT is not rolled back | `meetings.rs:386-388`; `chat.rs:927-940` |
| server request lacks object `meeting` | `space-server.rs:3895-3904`, binder `2996-3014` |

Server paths are present: policy `space-server.rs:2579`, organizer rebind `3895-3903`, typed dispatch `5328`; desktop handler includes it `lib.rs:504-510`.

## 3. Most likely native-side cause + falsification

**Hypothesis (not proven): non-atomic post-insert `attach_meeting_channel_on` is the only create-specific second operation.** With `channel_id=null` (all normal composers), `create_meeting` INSERTs first, then creates/reads `meeting:<id>` channel. If that second step fails (e.g. schema/database inconsistency), caller receives failure although meeting row already exists: `meetings.rs:385-388`, `chat.rs:934-940`. This is stronger than a missing dispatcher/serde/schema-field hypothesis: registration and minimum server payload already pass.

Decisive regression test→construct a migrated DB, force `create_entity_channel_impl`’s second operation to fail after the meeting INSERT (e.g. trigger abort on `channels` insert), call `create_meeting` with `channel_id=None`, assert: current behavior returns Err **and meeting persists**. Then require desired behavior: either transaction rollback (no meeting) or successful create with recoverable channel attachment. Add this beside meeting unit tests; independently run existing HTTP identity tests at `space-server.rs:9203-9218`, `9353-9363`, `9864-9873` to establish normal server create succeeds with legacy/minimal payload and organizer rebinding.

Existing evidence→three server integration cases assert HTTP `create_meeting` OK, including an intentionally sparse payload; therefore no native/server reproduction has been measured here. UNVERIFIED: tests not executed; no live application/port used.
