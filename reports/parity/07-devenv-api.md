# 07 — Dev Environments & Applications API parity audit

Scope: current `src/`, `src/api/`, `src-tauri/src/`; KB §4 gap analysis excluded. `done` requires Rust command wired in `tauri::generate_handler!`, a `src/api/*.ts` caller, and a rendered view. `stub` = persisted/backend declaration but no complete command→API→view path. `src-tauri/src/applications.rs` commands are handler-wired (`src-tauri/src/lib.rs:88-121`), `src/api/applications.ts` wraps them, and `Applications` is statically registered (`src/App.tsx:42`), so rows that were `stub` on the unwired-backend grounds are re-measured against that live path.
WIP:☀ `feat/tree-devenv-api` — devfile/deep-link + apps UI + webhook delivery/retry; 2026-08-22.

|feature|src(KB/legacy)|status|evidence|note|
|---|---|---|---|---|
|Cloud VM + Docker dev environment / Fleet-Gateway thin client|KB §3.1 #1|missing|`src-tauri/src/applications.rs:1-5`; `src/App.tsx:37-44`|Module explicitly excludes remote-machine provisioning; no VM/container view.|
|Devfile config: instance/IDE/image/env/parameters/secrets|KB §3.1 #2|partial|`src-tauri/src/applications.rs:45-73`; `src-tauri/src/lib.rs:88,93-94`; `src/api/applications.ts:3,22`; `src/views/Applications.tsx:23`|Devfile CRUD is handler→API→view wired and path-validated, but content is opaque YAML text; no instance/IDE/image/env/parameter/secret schema.|
|Multiple/selectable or generated devfiles|KB §3.1 #3|partial|`src-tauri/src/applications.rs:45-73`; `src/api/applications.ts:3`; `src/views/Applications.tsx:10,23`|Per-project devfile list renders and multiple `.space/*.devfile.yaml` rows persist; the UI writes one fixed default path and never sets `generated`, so no selection or generation flow.|
|Warm-up snapshots, schedule/git-push filters, script/indexing|KB §3.1 #4|missing|`src-tauri/src/applications.rs:1-417`; `src-tauri/src/db.rs:271-280`|No warm-up model, trigger, snapshot, command, API, or view.|
|Idle hibernation with persisted home/work tree|KB §3.1 #5|partial|`src-tauri/src/devenv.rs:1-330`; `src-tauri/src/db.rs` (SCHEMA_V55 `dev_environments`); `src/api/devenv.ts`; `src/views/DevEnvironments.tsx`|Per-environment idle timeout (default 30m), activity touch, idle sweep that hibernates and records the preserved home/working trees, and a resume that refuses a hibernation without a snapshot. Tests: `devenv::tests::idle_environment_hibernates_and_resumes_from_its_preserved_trees`. Lifecycle record only — no container stop/VM release, since no provisioner exists in this tree.|
|Standby/hot-pool claim|KB §3.1 #6|partial|`src-tauri/src/devenv.rs:250-300`; `src/views/DevEnvironments.tsx`|Standby environments are created unowned; `claim_standby_dev_environment` takes the oldest one through a conditional UPDATE (a race cannot hand the same environment to two members), enforces `Project.JoinHotPoolDevEnvironments`, and transfers ownership. Test: `devenv::tests::claiming_a_standby_environment_transfers_ownership_exactly_once`. No pre-warming of real machines.|
|Share a running dev environment|KB §3.1 #7|missing|`src/api/chat.ts:58-100`; `src/views/Chat.tsx:42-300`|Native chat exists; no dev-environment sharing entity/action.|
|Instance catalog, cloud policy, org default IDE|KB §3.1 #8|missing|`src-tauri/src/platform.rs:270-319`; `src/views/Admin.tsx:15-177`|Generic admin rights only; no RD catalog/policy/default-IDE records.|
|Dev-env/host-IDE/warm-up debug log pages|KB §3.1 #9|missing|`src/App.tsx:37-44`; `src-tauri/src/applications.rs:1-417`|No environment log/troubleshoot commands or routes.|
|Ten distinct DevEnvironments rights|KB §3.1 #10|partial|`src-tauri/src/rights.rs:26-120`; `src-tauri/src/platform.rs:270-319`; `src/views/Admin.tsx:129-177`|All ten rights are in the catalog under the `Dev Environments` group with their `Rd.*` codes in the description and the KB implication edges (`Unattended.Join`→`Create`→`Settings.View`, `Manage`→`View`, `Settings.Edit`→`Settings.View`). Create/manage/join are enforced in `devenv.rs`; connect/warmup/policy/debug rights have no consuming surface yet.|
|Project feature gate depends on repositories|KB §3.1 #11|missing|`src-tauri/src/db.rs:187-220`; `src/views/ProjectSettings.tsx:1-220`|No project-feature data/gate.|
|RD routes: configurations CRUD, environments/details/timeline, open-in-IDE, logs|KB §2.1 routing|partial|`src-tauri/src/applications.rs:45-91`; `src-tauri/src/lib.rs:88,93-95`; `src/views/Applications.tsx:23-24`; `src/router.ts:25-34`|Devfile CRUD and open-in-IDE are reachable inside the single `Applications` view; no dedicated RD routes, environment details/timeline, or log pages.|
|RD permission group: create/manage/view/join/connect/warmup/settings/policy/debug|KB §2.1 rights|partial|`src-tauri/src/rights.rs:26-120,410-440`; `src-tauri/src/platform.rs:455-614`; `src/views/Admin.tsx:129-177`|The named group is seeded and resolvable through the existing role matrix and implication walker; enforcement so far covers create/manage/join only.|
|`DEV_ENVIRONMENTS` project feature and DevEnvironment pin/menu hooks|KB §2.1 feature/pin/hooks|missing|`src/router.ts:25-34`; `src/nav.ts:10-20`; `src-tauri/src/db.rs:187-220`|No feature flag, pin kind, start-coding hook, or admin navigation item.|
|Local IDE connections/opened-repository state|KB §2.1 `IdeConnectionId`/`OnlineIde`|partial|V45 `ide_connections`/`ide_opened_repositories`; `src-tauri/src/applications.rs`; `src-tauri/src/lib.rs`; `src/api/applications.ts`; `src/views/Applications.tsx`|Desktop integrations report local sessions and opened repositories; Applications offers each discovered repository for click-gated IDE handoff. No cloud environment is inferred.|
|Five app extension surfaces: HTTP API, webhooks, chatbot commands, UI extensions, Kotlin/.NET SDK|KB §3.2 #1|partial|`src-tauri/src/bin/space-server.rs` (`/api/app/me`, `/api/app/projects`, RFC6750 bearer); `src/api/applications.ts`; `src/views/Applications.tsx`|Issued app bearer tokens now resolve app identity and read projects through inbound HTTP; webhook/chatbot/UI registration remains wired, but typed payload callbacks and Kotlin/.NET SDK are absent.|
|Four app types and five installation paths|KB §3.2 #2|partial|`src-tauri/src/applications.rs:184-196,731,951-975`; `src/api/applications.ts:5,13,24`; `src/views/Applications.tsx:20,35`|All four types are selectable in the register form and all five install kinds persist with uninstall; only MARKETPLACE/MANUAL are triggerable in the UI and JENKINS/TEAMCITY have no connector.|
|App OAuth flows and endpoint-authenticity credentials|KB §3.2 #3|partial|`src-tauri/src/applications.rs` (rotate_app_secret/issue_app_token/verify_app_token); `src-tauri/src/bin/space-server.rs` (`/api/app/*`); `src/api/applications.ts`; `src/views/Applications.tsx`|client_credentials token is now consumed by the RFC6750 app HTTP API (identity + read-scoped projects), with argon2-hashed secret+token, TTL, rotation-revokes-tokens, revoke/list; signing/keystore credentials remain absent.|
|Connection-health state, init error, healthy ping, force remove|KB §3.2 #4|partial|`src-tauri/src/applications.rs:184-217`; `src-tauri/src/lib.rs:96-98`; `src/views/Applications.tsx:35`|The four-state status is validated, wired, and rendered per application, and delete removes an app; no health monitor, HTTP init-error record, healthy ping, or force-remove semantics.|
|Typed app endpoint payload dispatcher|KB §3.2 #5|done|`src-tauri/src/payload_dispatch.rs`; `src-tauri/src/applications.rs` (`ApplicationPayload`, `sign_app_payload`); `src/api/applications.ts`; `src/views/Applications.tsx`|`dispatch_application_payload` parses the typed family, signs `{ts}.{body}` with the app’s Ed25519 key and POSTs it to the registered endpoint with the five `x-gaia-space-*` headers; unknown `className` and endpoint-less apps are rejected before the network. Egress is re-validated at dispatch time: loopback/private/link-local/CGNAT destinations and plaintext `http://` are refused (env switches `GAIA_APP_DISPATCH_ALLOW_PRIVATE_ENDPOINTS` / `GAIA_APP_DISPATCH_ALLOW_PLAINTEXT`), and a credential-bearing payload is never sent over plaintext. Freshness window `GAIA_APP_PAYLOAD_MAX_AGE_SECS` (default 300s) via `verify_fresh_app_payload`; receiver procedure in `docs/webhook-receiver-guide.md`. All five commands are dispatched in `src-tauri/src/bin/space-server.rs` under `CommandPolicy::AppAdmin` (`space-server::tests::app_credentials_are_admin_only` covers member refusal + admin reachability in the web build). Real-socket proof: `payload_dispatch_tests::a_real_post_carries_headers_a_receiver_can_verify_independently` POSTs through the production transport to a listener on an ephemeral port and verifies the headers and signature receiver-side; plus `..::a_stale_or_future_timestamp_is_refused_even_with_a_valid_signature`, `..::the_egress_guard_refuses_internal_addresses_and_plaintext_secrets`, `..::a_dispatch_to_an_internal_endpoint_never_leaves_the_process`, `..::every_payload_class_round_trips_and_dispatches_signed`. Command→API→view complete.|
|Richly filterable cross-domain webhooks|KB §3.2 #6|partial|`src-tauri/src/events.rs:1-80`; `src-tauri/src/applications.rs:277-298,318-343,461-608`; `src-tauri/src/issues.rs:402,418,431`; `src-tauri/src/documents.rs:417-420`; `src-tauri/src/git.rs:295-320`; `src-tauri/src/review.rs:172,182,349`|Dot-path/event filters are parsed and enforced at save and at delivery, with signed HTTP delivery, attempts, backoff retry, queue sweep, and history; a closed `domain.action` taxonomy (`events::EVENT_TAXONOMY`, `list_event_types`) is now emitted by real writes in four domains (issue, document, git commit, review), so subscriptions fire without a hand-fired payload; still no package/pipeline/chat events and no typed payload contracts.|
|Flat slash commands plus typed menu parameter forms|KB §3.2 #7|partial|`src/api/chat.ts:54-101`; `src-tauri/src/chatbot.rs`; `src/chatCommands.ts`; `src/views/Chat.tsx`|The channel composer now opens a slash menu that asks every registered chatbot endpoint for its commands (debounced, prefix-filtered, live answer preferred over the declared fallback); the typed `ExtensionActionParametersForm` for menu actions is still absent, so the row stays partial. Tests: `src/chatCommands.test.ts`.|
|UI extension pages/menu/context actions with scoped enablement|KB §3.2 #8|partial|`src-tauri/src/applications.rs:649-689`; `src-tauri/src/lib.rs:119-121`; `src/views/Applications.tsx:35`|Seven extension-point types are validated and registered/listed through the wired path; nothing renders the iframe, and enablement is one row-level flag with no action contexts or scopes.|
|Two-stage app required-rights vs context-authorized-rights|KB §3.2 #9|done|`src-tauri/src/app_rights.rs`; `src-tauri/src/bin/space-server.rs` (`/api/app/projects`, `/api/app/projects/{id}/issues`); `src/views/Applications.tsx`|Declaration, per-context grant and approval status exist and the external app API now enforces them: `/api/app/projects` answers only with projects the app holds `Project.ViewProject` in (project or org grant) and the app issue write refuses without `Project.CreateIssues`. Test `space-server::tests::the_app_api_shows_and_writes_only_what_the_rights_model_authorized`.|
|Parent/child app ownership|KB §3.2 #10|missing|`src-tauri/src/applications.rs:128-218`; `src-tauri/src/db.rs:274`|Application record has no owner/owner-app relation or mutation.|
|Featured integration presets (Jenkins/TeamCity/Jira/YouTrack/Slack)|KB §3.2 #11|partial|`src-tauri/src/applications.rs:184-196,731,908-951`; `src/views/Applications.tsx:19-20,35`|`FeaturedIntegration` is selectable and a generic marketplace listing can be installed; no named Jenkins/TeamCity/Jira/YouTrack/Slack presets or per-vendor install flow.|
|`ES_App` application registry/entity and create/update/get/list/count API|KB §2.2 core application entity/API|partial|`src-tauri/src/applications.rs:128-218`; `src-tauri/src/lib.rs:96-98`; `src/api/applications.ts:5`; `src/views/Applications.tsx:10,35`|List/upsert/delete are command→API→view complete; no get-by-id or count, and most `ES_App` fields/methods are absent.|
|Per-app OAuth settings and endpoint URI/SSL/auth configuration|KB §2.2 `createApp`/`updateApp` fields|partial|`src-tauri/src/applications.rs:141-208`; `src/api/applications.ts:5`; `src/views/Applications.tsx:35`|Endpoint URI (HTTP(S)-validated) plus client-credentials/code-flow/PKCE flags persist through the wired path and the URI is editable in the form; the flags have no UI control and endpoint SSL/auth options are absent.|
|App secrets/tokens, signing/public keys, SSH/GPG keys|KB §2.2 application API methods|partial|V52 `app_signing_keys`; `src-tauri/src/payload_dispatch.rs` (`app_signing_key`/`rotate_app_signing_key`); `src-tauri/src/applications.rs` (argon2 app secret/token); `src/views/Applications.tsx`|Client secret/token issue-verify-revoke plus a per-app Ed25519 signing pair: the public key and the retired previous public key are readable and rotatable through the wired path, the private key never leaves the table (`payload_dispatch_tests::rotation_publishes_a_new_key_and_keeps_the_previous_public_key`, `..::the_private_key_is_never_exposed_through_the_public_view`). V53 `app_ssh_keys`/`app_gpg_keys`: public-only add/list/delete, stable SHA-256 fingerprints, and durable GPG revocation; private material is rejected.|
|App archive/restore and owner profile/owner-app mutation|KB §2.2 application API methods|partial|`src-tauri/src/applications.rs:184-208`; `src-tauri/src/lib.rs:97`; `src/api/applications.ts:5`|The `archived` flag round-trips through the wired save command, so archive/restore is expressible via the API; no UI control and no owner-profile/owner-app relation or mutation.|
|App connection status, HTTP init error, delivery history/header masking|KB §2.2 `AppConnectionStatus`/delivery methods|partial|`src-tauri/src/applications.rs:184-217,461-608`; `src-tauri/src/lib.rs:96-98,109-115`; `src/views/Applications.tsx:31,35`|Status renders per app and delivery history carries attempts, response status, and last error through the wired path; no HTTP init-error record and no request/response header capture or masking.|
|`ApplicationType`, `FeaturedIntegrationType`, `AppKinds` enums|KB §2.2 enums|partial|`src-tauri/src/applications.rs:184-196`; `src/api/applications.ts:5`; `src/views/Applications.tsx:35`|`ApplicationType` is validated in Rust, typed in the API, and offered as a select; `FeaturedIntegrationType` and `AppKinds` have no representation.|
|`AppInstallInfo` marketplace/link/manual/Jenkins/TeamCity flows|KB §2.2 install model|partial|`src-tauri/src/applications.rs` (install_marketplace_app); `src-tauri/src/db.rs` SCHEMA_V31 `app_installs`; `src/views/Applications.tsx`|Install records carry all five kinds and uninstall; Jenkins/TeamCity flows have no connector logic.|
|Marketplace app metadata, capabilities, compatibility, installed-app refs|KB §2.2 `MarketplaceApp`|partial|`src-tauri/src/applications.rs` (marketplace CRUD); `src-tauri/src/db.rs` SCHEMA_V31 `marketplace_apps`; `src/views/Applications.tsx`|Name/vendor/capabilities/compatibility/listing URL plus installed-app refs; no remote marketplace fetch.|
|Generic app parameter key/value model|KB §2.2 `AppParameter`|missing|`src-tauri/src/db.rs:271-280`; `src/api/platform.ts:97-108`|Custom fields are unrelated; no app parameters table/API.|
|Typed `ApplicationPayload`: init, webhook, message, commands, menu/unfurl/custom, uninstall, external-issue|KB §2.2 payloads SDK|done|`src-tauri/src/applications.rs` (`ApplicationPayload`, `CLASS_NAMES`); `src/api/applications.ts`; `src/views/Applications.tsx`|All nine payloads are one serde `className`-tagged enum in Rust and the mirrored discriminated union in TypeScript, discoverable via `application_payload_classes` and dispatchable from the view; each variant round-trips through the canonical signed body (`payload_dispatch_tests::every_payload_class_round_trips_and_dispatches_signed`).|
|Webhook records, domain event taxonomy, typed subscription filters|KB §2.2 webhooks/subscriptions|partial|`src-tauri/src/events.rs:1-80`; `src-tauri/src/applications.rs:266-343,494-521,681-707`; `src-tauri/src/bin/space-server.rs:2413`; `src/views/Applications.tsx:26,35`|Subscription records and dot-path/event filters parse into a typed `WebhookFilter` enforced on save and on delivery; the domain event taxonomy is now modelled as a closed constant set with a `list_event_types` command and is emitted by issue/document/git/review write points (tests `applications::delivery_tests::a_review_write_enqueues_only_the_matching_subscription`, `..._a_git_commit_enqueues_the_matching_subscription`); filters remain untyped JSON dot-paths and the picker UI still takes a free-text event name.|
|Chatbot extension, `ListCommandsPayload`, flat `CommandDetail` list|KB §2.2 UI extensions/payloads|done|`src-tauri/src/chatbot.rs`; `src-tauri/src/chatbot_tests.rs`; `src/api/applications.ts`; `src/views/Applications.tsx`|`list_chatbot_commands` signs and POSTs a typed `ListCommandsPayload` to the bot's own endpoint, parses `Commands`/bare `CommandDetail` lists, filters by the typed prefix and falls back to the declared `commands_json` with the endpoint error reported.|
|Top-level/home/getting-started/issue-tracker/message/meeting/calendar UI extension declarations|KB §2.2 `AppUiExtensionApi`|partial|`src-tauri/src/applications.rs:660-689`; `src-tauri/src/lib.rs:119-121`; `src/views/Applications.tsx:35`|Top-level page, application homepage, getting started, menu item, chat-message menu item, meeting menu item, and external issue tracker are validated and registrable through the wired path; the calendar declaration is missing and no declaration is rendered anywhere.|
|Menu action contexts and typed extension parameter forms|KB §2.2 action contexts/forms|missing|`src-tauri/src/applications.rs:339-405`; `src/views/Chat.tsx:149-300`|No action-context/payload/form model.|
|Org/project/channel/per-user UI extension enablement state|KB §2.2 enabled state API|partial|`src-tauri/src/applications.rs:660-689`; `src-tauri/src/lib.rs:119-121`; `src/api/applications.ts:9`|A single row-level `enabled` flag round-trips through the wired save command; no org/project/channel/per-user scoping and no enabled-state query API.|
|Application required rights, authorized rights in context, request/approve scope|KB §2.2 `ApplicationRights`|done|`src-tauri/src/app_rights.rs`; `src-tauri/src/app_rights_tests.rs`; `src/api/applications.ts`; `src/views/Applications.tsx`|V56 `app_required_rights`/`app_authorized_rights`: declare/request (stage 1) is separate from per-context grant/approve/revoke (stage 2); `scope_approval_status` reports APPROVED/PARTIAL/PENDING plus grants that are no longer declared.|

## Totals

- done: 0
- partial: 27
- stub: 0
- missing: 15
- rows: 42

## Five worst gaps

1. No complete cloud Dev Environments path: devfile CRUD and click-gated local IDE handoff are wired, but VM/container lifecycle, environment detail/timeline, and IDE-session discovery remain absent.
2. Application registry/webhook/chatbot/extension CRUD is reachable; automatic domain-event delivery, chatbot callback dispatch, and concrete extension contracts remain incomplete.
3. App OAuth is client-credentials only and no external HTTP application API consumes the issued token; signing/SSH/GPG public-key records exist; app ownership is absent; client-credentials bearer tokens now resolve `/api/app/me` and read-scoped `/api/app/projects` with RFC6750 failures.
4. Webhook delivery, signing, history, and retry execute, but no domain write emits events automatically and there is no event taxonomy.
5. §20 route audit: generic rights are command→API→Admin complete, but the named RD permission group remains absent; it is a partial foundation, not an isolated RD route.

## Crown implementation note — 2026-08-22
- WIP:☀ completed · commits `b0dc34c` → `8e3a220`.
- Devfile/open-IDE: SQLite V14 + Rust IPC/web dispatch → `src/api/applications.ts` → `src/views/Applications.tsx`; no cloud VM lifecycle.
- Apps/webhooks/chatbots/UI extensions: registry + CRUD reachable in Applications view; Tauri handler and `/api/cmd/*` dispatch wired.
- Webhook delivery/retry status=partial: SQLite V15 history; manual JSON POST, 10s timeout/no redirects, response/error capture, exponential retry schedule + UI retry; replay contract `docs/webhook-receiver-guide.md:25-32`; durable ID header `src-tauri/src/applications.rs:352`.
- Evidence: `src-tauri/src/applications.rs`; `src-tauri/src/db.rs`; `src-tauri/src/bin/space-server.rs`; `src/api/applications.ts`; `src/views/Applications.tsx`.
- Verification status=done (manual retry path): `applications::delivery_tests::webhook_delivery_posts_then_retries_after_an_http_failure` asserts FAILED→SUCCEEDED / attempts 1→2 and durable delivery ID at `src-tauri/src/applications.rs:1044-1065`.
- w4 (feat/w4-hooks): background delivery ticker sweeps the retry queue automatically (`space-server::spawn_webhook_ticker`, `SPACE_WEBHOOK_TICK_SECS`/`SPACE_WEBHOOK_TICK_BATCH`, 0=off; unit `space-server::tests::webhook_ticker_config_defaults_and_optout`) ⇒ delivery no longer needs an operator click. Cross-domain fan-out now covers issues + documents (`documents::document_event`, test `applications::delivery_tests::a_document_write_enqueues_only_its_own_domain_subscription`); subscription filters are editable in the Applications view (event select + filters JSON) ⇒ the two filter/taxonomy rows stub→partial. Still partial, not done: only 2 of 8 domains emit events, no inbound app HTTP API/SDK. UNVERIFIED: ticker never observed against a live long-running server, only unit-level config + queue tests.
- Remaining: app OAuth/keys/marketplace/app-rights; native chatbot payload dispatch; automatic domain event emission; cloud dev environments.
