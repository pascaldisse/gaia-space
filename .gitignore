# Miscellaneous
*.log
*.swp
.DS_Store
.history

# IntelliJ / editors
*.iml
*.ipr
*.iws
.idea/
#.vscode/

# GAIA agent room/session data (local instance state, not project source)
.gaia/

# Node / Bun
node_modules/
dist/
dist-web/
bun.lockb.bak

# Rust / Cargo / Tauri
src-tauri/target/
# Cargo.lock IS committed (binary app, reproducible builds) -- do not ignore it.

# Local SQLite data (dev db, WAL/SHM files)
*.db
*.db-shm
*.db-wal
!src-tauri/migrations/**

# Legacy Flutter tree keeps its own .gitignore (legacy/flutter/.gitignore)
.scratch/
.work/

# Real financial data never enters this repository — see docs/finance-plan-format.md.
# The plan is imported at runtime; the numbers live outside the tree.
reports/finance/
docs/finance/
*finanzplan*
# GitHub push bridge delivery dedupe state; deployment-owned.
bridge/github-push/state.json
