# issue #5 終審 (Kali / L3審) — MERGE 可

worktree=/Users/pascaldisse/projects/gs-issue-5-opus · 枝 fix/gs-issue-5 · head=72e7719 · base=c6d0384

## 根因
`crypto.randomUUID()` = secure-context API。HTTP served web build → `crypto.randomUUID` undefined →
composer が `create_meeting` を送る前に throw。外側の一つの catch が「create 失敗」と表示 →
「meeting 作成が動かない」の正体。第二の病:meeting 保存後の `attach_meeting_channel` /
`invite_meeting_participant` の失敗を同じ外 catch が飲み、既に予約済みの meeting を「失敗」と偽った。

## 修復
- `src/api/ids.ts:1-17` — `newId()`:randomUUID→getRandomValues→時刻+乱数 fallback、`typeof crypto` guard。
- `src/views/Meetings.tsx:165-168,171` / `src/views/Calendar.tsx:198-201` / `src/components/WorkItemDrawer.tsx:126-128,131`
  — organizer transport 分岐:`organizer=profileId()||null`、`if(!organizer && !isWeb()) throw NO_ORGANIZER`。
  web=null送出→server rebind、desktop IPC のみ拒否。
- `src/calendar.ts:51-55` — `NO_ORGANIZER` 一文。
- 第二の act を各自 catch:`Meetings.tsx:197-199`、`Calendar.tsx:203-206`、`WorkItemDrawer.tsx:135-142`
  (invite 失敗時 `return` → `props.onClose()` 到達せず、error 残置)。
- server 側 (既存): `space-server.rs:2579` `create_meeting`→`CommandPolicy::SessionIdentityWrite`、
  `:3895-3901`→`bind_required_object_identity`(`:2996-3015`)= organizer_id を session profile で**上書き/挿入**。
  ∴ null 送出は web で安全。

## transport 判定の照合 (審の主眼)
`session.ts:8` `isWeb()= online && window有 && (!isTauriRuntime() || isMobileServer())`
`api/invoke.ts:51-53` HTTP経路= `!isTauri() || !canInvoke() || isMobileServer()`
- 危険向き(isWeb真だが IPC 送出)= **存在せず**:isWeb真の両枝とも invoke は HTTP。∴ organizer null の invisible meeting は発生しない。BLOCKER 閉鎖。
- 逆向きの不一致は残る → 下記 MEDIUM。

## test 対応 (裸検査)
- `src/views/meetings.create.test.tsx:100` UUID欠如でも create_meeting 送出 / `:114` secure context 同一 / `:127` attach 拒否でも create 保持
- `src/views/meetings.organizer.test.tsx:55` web=null organizer 送出 / `:75` desktop IPC 拒否+文言
- `src/components/workitemdrawer.test.tsx:90` invite 拒否→drawer 留・error 可視 (`closed===0`)
- `src-tauri/src/meetings.rs` `create_meeting_accepts_the_composer_payload`
- `src-tauri/src/bin/space-server.rs` `the_composer_sequence_creates_a_meeting_a_person_can_then_see`

裸(巻き戻しても赤くならぬ)= `Calendar.tsx:198-201` organizer 分岐 · `Calendar.tsx:203-206` attach catch ·
`WorkItemDrawer.tsx:127-128` organizer 分岐 (test は profileId="me" 固定)。→ 既 test 済 Meetings 経路の写し、
挙動同型・低risk。MERGE 阻却せず、既知欠陥として引渡す。

## gate (審が自手で全走)
```
$ bunx tsc --noEmit
EXIT=0

$ bun test
 498 pass
 0 fail
 1722 expect() calls
Ran 498 tests across 73 files. [44.16s]

$ cd src-tauri && cargo test --no-default-features --lib
test result: ok. 433 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 130.79s
EXIT=0

$ cargo test --no-default-features --bin space-server   (追加: bin test は --lib に含まれぬ)
test tests::the_composer_sequence_creates_a_meeting_a_person_can_then_see ... ok
test result: ok. 77 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 76.31s
```
`git status --short` = 空 (本ファイル作成前) · `git diff c6d0384..HEAD --stat` に node_modules 無し (24 files, +741/-23)。

## 未修復既知欠陥
1. MEDIUM `session.ts:8` vs `api/invoke.ts:51-53`:`__TAURI__` のみ在り `__TAURI_INTERNALS__` 無しの頁は
   HTTP 送出だが `isWeb()`=false → profile 未選択時に誤って NO_ORGANIZER で拒否。向きは fail-safe
   (誤 refuse であり invisible meeting ではない)。恒久解=transport 述語を invoke.ts と共有せよ。
2. MEDIUM 裸修復三箇所 (上記) — Calendar composer の test が皆無。
3. LOW `ids.ts:2` fallback は暗号強度無し (衝突耐性のみ)。id は非secret ∴許容、但し明記。
