# 審 issue#5 (Kali·L3審·読専) — 自説殺し

head審時 = `f20e46f` (建者commit) + 未commit差分 (meetings.create.test.tsx / Calendar.tsx / WorkItemDrawer.tsx)。

## (a) 非secure context の実在性 — 判定=**真(潜在に非ず)**
証拠(repo内のみ):
- `src/mobile.ts:18` — `if (url.protocol !== "https:" && url.protocol !== "http:") throw` → **http:// のサーバURLを明示的に許容**。
- `src-tauri/src/lib.rs:658-659` — 同一律をnative側でも許容 (`scheme() != "https" && != "http"` のみ拒否)。
- `src/api/invoke.ts:41-46` `isMobileServer()` — mobile shell が `tauri.localhost` 以外の remote page に居る事を前提に設計済 → その page が http なら **非secure context**、`crypto.randomUUID===undefined`。
- `vite.config.ts:18` `base:"/space/"` + `deploy/Caddyfile.space` = handle片のみ(scheme宣言無し) → https保証の証拠**無し**。TLS強制の証拠は repo 内に不在 (`deploy/rollout.sh:118` の gate も `http://127.0.0.1:8090`)。
- 反証側: localhost/127.0.0.1 は secure context 扱い → dev では再現せず。∴ 「本番httpで配る」直接証拠は無いが、**製品が http サーバURLを受理する経路を自ら実装している**以上、降格は不要。判定=真の原因(条件付き:http配信/http mobile server時)。UNVERIFIED=実際の本番配信scheme(repo外)。

## (b) 生存欠陥一覧
1. **未ガードの `crypto.randomUUID` が8箇所残存**(同一根因、Meetings系のみ修復):
   `src/api/pipelines.ts:7` · `src/api/chat.ts:318` · `src/api/review.ts:7` · `src/api/documents.ts:7` · `src/views/Projects.tsx:48` · `src/views/IssueDetail.tsx:60` · `src/views/CallPanel.tsx:201` · `src/views/DevEnvironments.tsx:14`。→ 非secure contextでは chat送信・文書作成・project作成・添付・pipeline が同じ様式で落ちる。
2. 仮説(a) organizer null → 不可視: **部分的に真**。`MEETING_READ_SCOPE` (`src-tauri/src/meetings.rs:189`) は `m.organizer_id=?1` / participants / project channel のみ許可 → null organizer + visibility=participants は作成者にも不可視、行は残る。ただし旧 `Meetings.tsx:172` は `draft.organizer_id || profileId() || null` で、drawer の ProfilePicker が organizer を持つ場合 null に到達しない → **issue#5 の主症状(「作成が効かない」)の説明としては弱い**(不可視は静かな別欠陥)。
3. 仮説(b) 非transactional post-insert (`meetings.rs:385-388`): **真**。`attach_meeting_channel_on` 失敗時 INSERT 済み行は残留(rollback無し)し、`create_meeting` は Err を返す → client 側は「作成失敗」表示なのに行は存在。建者の client 側 try/catch は症状を隠すが、**server の非原子性そのものは未修正**(commandが Err を返す経路では今も残留)。
4. `newId()` fallback は **RFC4122 v4 ではない**(`ts-8hex-8hex`、version/variant nibble 無し)。`meetings.rs` に UUID 形式検証は無い(`grep uuid` = 0 hit)ので現状は無害だが、将来 server 側で UUID 型を課すと破綻。衝突確率: 同一ms内で 2×32bit → 実用上無視可(局所row id、認証に不使用)。

## (c) 修復diffの欠陥/漏れ
- 網羅漏れ: (b)1 の8 callsite。`src/api/ids.ts` を作った以上、置換しない理由が無い。**マージ条件**。
- `Applications.tsx:23` の旧inline guard が `ids.ts` に統合されていない(重複規則、二源)。
- **未commit test が現在 FAIL**(下記gate)。`no profile means no create_meeting` が `create_meeting` を送ってしまう = drawer の organizer 既定値(ProfilePicker由来)が `draft.organizer_id` を埋めるため。∴ test の前提かproductionの refuse 位置(drawer既定値の扱い)いずれかが誤り。**マージ阻止条件**。
- 退行捕捉力: `meetings.create.test.tsx:37` が `crypto.randomUUID` を undefined に差し替え、`create_meeting` 送出を主張 → 修復を巻き戻せば `Meetings.tsx` 旧 `crypto.randomUUID()` が TypeError → 送出0 → fail。**退行を真に捕らえる(思考実験、編集せず)**。
- ただし test は Meetings 面のみ。Calendar/WorkItemDrawer/他8 callsite の非secure context 退行は無防備。
- server 非原子性 (b)3 の test/修復 無し。

## (d) gate貼
```
$ bunx tsc --noEmit   → exit 0 (エラー0)
$ bun test src/views/meetings.create.test.tsx src/views/meetings.view.test.tsx src/calendar.view.test.tsx src/calendar.test.ts
 34 pass / 1 fail / 157 expect
 (fail) a meeting whose organizer cannot be named is refused, not written
        > no profile means no create_meeting, and the reason is on screen
   expect(calls.some(c=>c.cmd==="create_meeting")).toBe(false) → Received: true
```
(注: f20e46f 単体 = 18 pass / 0 fail。fail は建者の未commit WIP 由来。)

## (e) マージ可否
**現状=否**。条件:
1. 赤test を緑に(production側で drawer 既定 organizer を扱うか、test前提を正す — どちらが正しいか建者が明言せよ)。
2. 残8 callsite を `newId`/`prefixedId` に置換 + `Applications.tsx:23` 統合、`grep -rn "crypto.randomUUID" src` が ids.ts のみに一致。
3. `meetings.rs` の insert+channel を transaction 化(または残留行の説明をcommitに記す)。最低限 UNVERIFIED として明記。
4. gate: `bunx tsc --noEmit` 0 + `bun test` 全緑を貼る。
