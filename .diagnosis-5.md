# issue #5 「create meeting doesn't work」— 診断(修復未着手)

## 断定 = 失敗path一本

`crypto.randomUUID()` は **secure context 専用**。web build は `http://<host>/space/` で供される
= 非secure context ⇒ `crypto.randomUUID` は `undefined` ⇒ composer の `create` 一行目で
`TypeError` ⇒ catch → error行表示、**IPC/HTTPは一度も発火しない**。
「作成が効かない・けれど何も届いていない」= 報告そのもの。

証跡(真=碼読/実測):
- `src/views/Meetings.tsx:165` — `id: crypto.randomUUID(),` 無防備(create の最初の式)。真
- `src/views/Calendar.tsx:198` — 日composer も同式 `id:crypto.randomUUID()`。同一欠陥。真
- `src/components/WorkItemDrawer.tsx:124` — `const id = crypto.randomUUID();` 同族。真
- `src/views/Applications.tsx:23` — `crypto.randomUUID?.() ?? \`${Date.now()}-${Math.random()...}\``
  = **同じ欠陥が過去に一度踏まれ、一箇所だけ守られている**証拠。真
- `src/api/invoke.ts:54-60` — web transport は `fetch("{BASE}api/cmd/<cmd>")`。TLS前提なし。真
- 再現test: `src/views/meetings.create.test.tsx` — `crypto.randomUUID` を消した頁で
  「New meeting → 題名 → 送信」→ `create_meeting` が **一度も呼ばれない**(下記出力)。真

## 検証済 = 無罪の層(独立path・実測)

1. **rust `create_meeting` は composer payload をそのまま受ける**
   `src-tauri/src/meetings.rs` test `create_meeting_accepts_the_composer_payload`:
   composer が送る **JSONそのもの** を serde 経由で通し、migrate 済DBへ INSERT 成功。→ 仮説①②④ 死。
2. **web transport 全系列も健全**
   `src-tauri/src/bin/space-server.rs` test `the_composer_sequence_creates_a_meeting_a_person_can_then_see`:
   `create_meeting` → `attach_meeting_channel` → `invite_meeting_participant` → `list_meetings`
   を実routeで順に叩き、全て 200・作成物が list に返る。→ 仮説③ 死。

## 死枝(棄却仮説 + 理由)

- ① TS `Meeting` 必須fieldをcomposerが送らず serde missing field
  死: `Meetings.tsx:163-186` / `Calendar.tsx:198` / `WorkItemDrawer.tsx:125-132` は
  22field全てを明示送出。かつ payload実物を serde に通すtestが緑。
- ② rust `Meeting` struct の field名/型 mismatch(`video_*`/`source_entity_*`/`meeting_url`)
  死: 同test緑。`meetings.rs:96-139` の `#[serde(default)]` 群と TS型は一致。
  INSERT 17列/17 params も一致(`meetings.rs:385`)。
- ③ `bind_required_object_identity` が organizer_id を要求/拒否
  死: `space-server.rs:2996-3014` は key不在でも **挿入する**(拒否しない)。
  `organizer_id:null` でも 200、session profile に rebind される(既存test + 新e2e test 緑)。
- ④ DB列不足(migration)
  死: `db.rs:974-975`(visibility/modification_preference)・`888-889`(source_entity_*)・
  `1015`(meeting_url)で全列存在。migrate 済DBへの実INSERT が緑。
- ⑤ `create_meeting` が張った channel を client が再度 `attach_meeting_channel` して衝突
  死: `chat.rs:934` は `INSERT OR IGNORE` = 冪等。e2e test で二重呼び出しも 200。
- ⑥ desktop に command 未登録(この repo の既往症)
  死: `lib.rs:506,510,516` に `create_meeting`/`attach_meeting_channel`/`invite_meeting_participant` 登録済。

## 影響範囲(修復atomへの申し送り、まだ書かない)

非secure context で id を鋳造する全surface:
`Meetings.tsx:165` · `Calendar.tsx:198` · `WorkItemDrawer.tsx:124` ·
`api/pipelines.ts:7` · `api/chat.ts:318` · `api/review.ts:7` · `api/documents.ts:7` ·
`views/Projects.tsx:48` · `views/IssueDetail.tsx:60` · `views/CallPanel.tsx:201` ·
`views/DevEnvironments.tsx:14`。
既に守られているのは `views/Applications.tsx:23` のみ。
⇒ 修復は「共有 id 鋳造器を一つ」であって、`Meetings.tsx` 一行の `?.()` ではない(假=設計判断、次atomの担当)。

## UNVERIFIED

- 実配備が本当に平文HTTP で供されているか(TLS終端の有無)は **本worktreeからは未測**。
  生アプリ起動禁のため port/配備の実地確認はしていない。
  ただし secure context でない頁でのみ壊れる事、および `Applications.tsx:23` の既存防御は 真。
- desktop(tauri webview, `tauri://localhost`)は secure context 扱いのため本欠陥に触れない。假(仕様知識、実測せず)。
